@AGENTS.md

# Claude Code — Platform Deltas

Read `AGENTS.md` and `docs/ai/DOCKLIST_OPERATING_SYSTEM.md` before any task.

- Skills live in `.claude/skills/`. Invoke via the `Skill` tool.
- Use `/memory` to verify project instructions are loaded if unsure.
- `@`-includes (such as `@AGENTS.md` above) are Claude-only; treat them as transparent includes when they appear in repo docs.

Workflow, declaration, completion report, non-negotiables, and guardrails are defined once in `docs/ai/DOCKLIST_OPERATING_SYSTEM.md` and `docs/ai/snippets/`. Do not restate them in prompts.
