/**
 * Backwards-compat re-exports.
 * The canonical Docklist UI system now lives in `src/components/dl.tsx`.
 * Keep these named exports so existing route imports continue to work.
 */
export { AppShell, PageHeader, DashboardCard, Card } from "@/components/dl";
